export const environment = {
  production: true,
  // Vacío = mismo origen que la app (se recomienda servir la API detrás de HTTPS
  // en el mismo dominio o mediante un proxy inverso).
  apiBaseUrl: '',
};
